
boolean canMove;
int x = 0;
int y = 100;

void setup() {
size(200, 200);
canMove = false;
}

void draw() {
if (canMove){ x=1;
  background(127);
circle(x, y, 50);
if (canMove){
;
}
}

if (canMove) {
canMove = true;
}else {
  canMove = false;


}}
